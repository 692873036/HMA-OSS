<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.shipook.reader.mfxsdq"> <!-- 同步第一份包名 -->

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="PackageVisibilityPolicy,QueryAllPackagesPermission" />

    <application
        android:name="icu.nullptr.hidemyapplist.MyApp"
        android:allowBackup="true"
        android:fullBackupOnly="true"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="听书神器" <!-- 同步第一份应用名称 -->
        android:theme="@style/AppTheme"
        tools:targetApi="33">

        <activity
            android:name="com.shipook.reader.mfxsdq.ui.activity.MainActivity" <!-- 同步包名对应的Activity路径 -->
            android:exported="true"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="de.robv.android.xposed.category.MODULE_SETTINGS" />
            </intent-filter>
        </activity>

        <activity-alias
            android:name="com.shipook.reader.mfxsdq.MainActivityLauncher" <!-- 同步包名对应的Activity-alias路径 -->
            android:exported="true"
            android:targetActivity="com.shipook.reader.mfxsdq.ui.activity.MainActivity"> <!-- 同步目标Activity路径 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <provider
            android:name="icu.nullptr.hidemyapplist.service.ServiceProvider"
            android:authorities="com.shipook.reader.mfxsdq.ServiceProvider" <!-- 同步包名对应的authorities -->
            android:exported="true"
            tools:ignore="ExportedContentProvider" />

        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Reject applist detection." />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />
    </application>
</manifest>
